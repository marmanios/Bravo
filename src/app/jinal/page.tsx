'use client';

import React from 'react';
import LiveTranscription from '@/components/ui/live-transcription';

const JinalPage: React.FC = () => {
  return (
    <div>
      <LiveTranscription />
    </div>
  );
};

export default JinalPage;